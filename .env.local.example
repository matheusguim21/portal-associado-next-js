NEXT_PUBLIC_APP_ENV=development

# Client-visible URLs (OAuth redirect / logout still need public auth URL)
# NEXT_PUBLIC_API_URL_DEV=http://localhost:8080
# NEXT_PUBLIC_AUTH_API_URL_DEV=http://localhost:8081
# NEXT_PUBLIC_CLIENT_ID=portal-associado

# Server-only (preferred in production; fall back to NEXT_PUBLIC_* when unset)
API_URL_DEV=http://localhost:8080
AUTH_API_URL_DEV=http://localhost:8081
CLIENT_ID=portal-associado
# Base64 de portal-associado:web123
CLIENT_CREDENTIALS=cG9ydGFsLWFzc29jaWFkbzp3ZWIxMjM=
